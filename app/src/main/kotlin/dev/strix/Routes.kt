package dev.strix

/** App route definitions. The fullscreen player lives inside the channels screen, so
 *  there is no separate player route. */
internal object Routes {
    const val ONBOARDING = "onboarding"
    const val CHANNELS = "channels"
    const val EPG = "epg"
}
