package dev.strix.core.database

/**
 * Skeleton marker for the `:core:database` module. Replaced by Room entities,
 * DAOs, and the FTS search table (see docs/BACKLOG.md).
 */
internal object CoreDatabaseModule
