package dev.strix.core.common

/**
 * Skeleton marker for the `:core:common` module. Replaced as real types land —
 * domain models, `Result`, dispatcher abstractions, repository interfaces
 * (see docs/BACKLOG.md).
 */
internal object CoreCommonModule
