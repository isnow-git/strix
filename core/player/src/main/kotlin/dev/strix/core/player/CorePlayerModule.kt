package dev.strix.core.player

/**
 * Skeleton marker for the `:core:player` module. Replaced by the Media3 wrapper,
 * custom LoadControl + ABR policy, and lifecycle-bound release
 * (see docs/BACKLOG.md).
 */
internal object CorePlayerModule
