package dev.strix.core.network

/**
 * Skeleton marker for the `:core:network` module. Replaced by the OkHttp client,
 * streaming M3U/EPG parser, retry + backoff, and circuit breaker
 * (see docs/BACKLOG.md).
 */
internal object CoreNetworkModule
