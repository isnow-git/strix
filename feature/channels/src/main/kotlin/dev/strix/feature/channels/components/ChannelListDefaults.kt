package dev.strix.feature.channels.components

/** Fixed metrics for the channel list; the screen reuses them to centre a zapped row. */
object ChannelListDefaults {
    const val ROW_HEIGHT = 64
    const val ROW_SPACING = 6
    const val LOGO_SIZE = 52
}
