package dev.strix.feature.channels

/**
 * Skeleton marker for the `:feature:channels` module. Replaced by the Paging 3
 * channel list, search, and MVI ViewModel (see docs/BACKLOG.md).
 */
internal object ChannelsModule
