package dev.strix.feature.onboarding

/**
 * Skeleton marker for the `:feature:onboarding` module. Replaced by the embedded
 * NanoHTTPD server, QR generation, encrypted credential storage, and the
 * onboarding MVI flow (see docs/BACKLOG.md).
 */
internal object OnboardingModule
