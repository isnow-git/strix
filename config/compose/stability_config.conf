// Types the Compose compiler should treat as stable but can't infer, because they
// live in pure-Kotlin modules the Compose compiler doesn't process (:core:common).
// They are immutable value/data classes, so marking them stable lets Compose skip
// recomposition by value equality instead of falling back to reference checks.
dev.strix.core.common.model.Channel
dev.strix.core.common.model.ChannelId
dev.strix.core.common.epg.NowNext
dev.strix.core.common.epg.EpgProgramme
