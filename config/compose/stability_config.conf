// Types the Compose compiler should treat as stable but cannot infer, because they
// live in the pure-Kotlin :core:model module (which the Compose compiler does not
// process). They are immutable value/data classes, so marking them stable lets
// Compose skip recomposition by value equality instead of reference identity.
// Extend this list as more :core:model types are passed into composables.
dev.strix.core.model.Channel
dev.strix.core.model.ChannelId
dev.strix.core.model.epg.NowNext
dev.strix.core.model.epg.EpgProgramme
